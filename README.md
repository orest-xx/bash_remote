use: in case you need to make copy you ssh pub key to remote hosts
put:
- hosts in hosts.txt
- passwords for remote user in passwords.txt

code use expect lib to prompt passwords, in case of macOs:

`brew install expect`

on linux:

`sudo apt update` 

`sudo apt install expect`
